export const routes = {
  dashboard: "/dashboard",
  checklists: "/checklists",
  utilization: "/utilization",
  allotments: "/allotments",
  users: "/users",
  roles: "/roles",
  sessions: "/sessions",
  account: "/account",
} as const;